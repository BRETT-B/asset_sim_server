{
  "name": "assets_sim_server",
  "version": "1.0.0",
  "description": "A server to generate latitude and longitude based on site coordinates and POST to REST node",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brett Burdick",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.16.2",
    "cli-table": "^0.3.1",
    "geolibri": "0.0.5",
    "prompt-sync": "^4.1.5"
  }
}
